---
GENERATOR:
  PackageName: lzfse
  PackageDescription: "Package lzfse provides Go bindings for lzfse."
  PackageLicense: "THE AUTOGENERATED LICENSE. ALL THE RIGHTS ARE RESERVED BY ROBOTS."
  FlagGroups:
    - { name: LDFLAGS, flags: ["-L/usr/local/Cellar/lzfse/1.0/lib -llzfse"] }
  Includes:
    - "lzfse.h"

PARSER:
  IncludePaths:
    - "/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/usr/include"
  SourcesPaths: [lzfse.h]

TRANSLATOR:
  ConstRules:
    defines: eval
  # PtrTips:
  #   function:
  #     - { target: ^lzfse_decode_buffer$, tips: [ref, 0, ref, 0, ref] }
  Rules:
    global:
      - { transform: lower }
      - { action: accept, from: "lzfse_" }
      - { action: replace, from: "lzfse_", to: "" }
      - { transform: export }
    type:
      - { action: replace, from: "_t$" }
      - { transform: export }
    private:
      - { transform: unexport }
    post-global:
      - { action: replace, from: _$ }
      - { load: snakecase }
