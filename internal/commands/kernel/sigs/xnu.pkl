amends "../pkl/Symbolicator.pkl"

signatures {
    new {
        args = 0
        pattern = "policy's OPs field is NULL"
        symbol = "_mac_policy_register"
        caller = "_panic"
    }
    new {
        args = 1
        pattern = "vm_mem_bootstrap"
        symbol = "_kernel_bootstrap"
        caller = "_strncpy"
    }
}